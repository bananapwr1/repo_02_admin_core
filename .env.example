# =============================================================================
# ADMIN CORE (REPO 02) - КОНФИГУРАЦИЯ
# =============================================================================
# Этот бот управляет всеми данными через Admin Panel.
# Требует Service Role Key для полного доступа к базе данных (обход RLS).
# =============================================================================
# ВАЖНО: По ТЗ проект должен требовать ТОЛЬКО эти 5 переменных окружения:
#   - SUPABASE_BASE_URL
#   - SUPABASE_SERVICE_KEY (или SUPABASE_KEY)
#   - SUPABASE_ENCRYPTION_KEY
#   - TELEGRAM_BOT_TOKEN
#   - ADMIN_USER_ID
# =============================================================================

# Telegram Bot Token (Admin Core)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# =============================================================================
# SUPABASE - БАЗА ДАННЫХ (ЕДИНАЯ ДЛЯ ВСЕХ КОМПОНЕНТОВ)
# =============================================================================
# КРИТИЧЕСКИ ВАЖНО:
# - Admin Core (Repo 02) использует Service Role Key (полный доступ)
# - UI/Trading Bot (Repo 01) использует Anon key (ограниченный доступ)
# 
# ГДЕ НАЙТИ КЛЮЧИ:
# 1. Откройте Supabase Dashboard
# 2. Выберите ваш проект
# 3. Settings -> API
# 4. Скопируйте:
#    - "URL" -> SUPABASE_BASE_URL
#    - "service_role" key -> SUPABASE_SERVICE_KEY (или SUPABASE_KEY)
#
# ВАЖНО: Service Role Key должен быть 200+ символов и начинаться с "eyJ"
# =============================================================================
SUPABASE_BASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here_200plus_characters
# альтернативное имя (если удобнее):
# SUPABASE_KEY=your_supabase_service_role_key_here_200plus_characters

# Ключ шифрования для конфиденциальных данных стратегий
# Сгенерировать можно командой:
#   python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SUPABASE_ENCRYPTION_KEY=your_32_byte_encryption_key_here

# Единственный ID администратора (Repo 02 должен быть доступен только ему)
ADMIN_USER_ID=123456789
