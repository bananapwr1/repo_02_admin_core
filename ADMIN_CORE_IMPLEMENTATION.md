# 📋 Реализация Admin Core - Документация

## 🎯 Обзор

Admin Core (Repo 02) - это компонент для управления торговыми стратегиями и системными уведомлениями. Использует полный доступ к базе данных через `SUPABASE_SERVICE_KEY` (или `SUPABASE_KEY`).

## ✅ Выполненные Задачи

### 1. Интеграция Telegram-Уведомлений ✅

**Файл:** `services/notification_service.py`

**Функциональность:**
- ✅ Отправка уведомлений администратору через `ADMIN_USER_ID`
- ✅ Уведомления о старте/перезапуске Admin Core
- ✅ Уведомления о критических ошибках
- ✅ Уведомления о создании/активации/деактивации стратегий
- ✅ Категоризация по уровням: INFO, WARNING, ERROR, CRITICAL

**Использование:**

```python
from services import get_notification_service

# Получить сервис
notification_service = get_notification_service(bot)

# Отправить уведомление о старте
await notification_service.notify_startup()

# Отправить уведомление об ошибке
await notification_service.notify_error("Описание ошибки", "ERROR_TYPE")

# Отправить уведомление о создании стратегии
await notification_service.notify_strategy_created("Strategy Name", strategy_id)
```

### 2. Реализация Логики Управления Стратегиями ✅

**Файл:** `services/strategy_manager_service.py`

**Функциональность:**
- ✅ Создание стратегий с шифрованием конфиденциальных данных
- ✅ Активация/деактивация стратегий
- ✅ Чтение стратегий с автоматической расшифровкой
- ✅ Использование `SUPABASE_SERVICE_KEY` (или `SUPABASE_KEY`) для всех операций
- ✅ Интеграция с сервисом уведомлений

**Шифруемые поля:**
- `api_keys` - API ключи бирж
- `secret_keys` - Секретные ключи
- `private_params` - Приватные параметры
- `credentials` - Учетные данные

**Использование:**

```python
from services import get_strategy_manager

# Получить менеджер стратегий
strategy_manager = get_strategy_manager()

# Создать новую стратегию с шифрованием
strategy_id = await strategy_manager.create_strategy(
    name="My Trading Strategy",
    description="Description of the strategy",
    is_active=True,
    assets_to_monitor=["BTC/USDT", "ETH/USDT"],
    timeframe="1h",
    indicators={"rsi": {"period": 14}, "ma": {"period": 50}},
    entry_rules={"rsi_below": 30},
    exit_rules={"rsi_above": 70},
    risk_management={"max_loss": 2.0},
    # Конфиденциальные данные (будут зашифрованы)
    api_keys={"binance": "your_api_key"},
    secret_keys={"binance": "your_secret_key"}
)

# Получить все стратегии (с расшифровкой)
strategies = await strategy_manager.get_all_strategies(decrypt=True)

# Получить активные стратегии
active_strategies = await strategy_manager.get_active_strategies()

# Активировать стратегию
await strategy_manager.activate_strategy(strategy_id)

# Деактивировать стратегию
await strategy_manager.deactivate_strategy(strategy_id)

# Переключить статус
await strategy_manager.toggle_strategy_status(strategy_id)
```

### 3. Сервис Шифрования ✅

**Класс:** `EncryptionService`

**Функциональность:**
- ✅ Шифрование/расшифровка строк и JSON
- ✅ Использование Fernet (symmetric encryption)
- ✅ Автоматическая обработка ошибок

**Использование:**

```python
from services.strategy_manager_service import EncryptionService

# Создать сервис
encryption = EncryptionService()

# Проверить доступность
if encryption.is_available():
    # Зашифровать строку
    encrypted = encryption.encrypt("sensitive_data")
    
    # Расшифровать строку
    decrypted = encryption.decrypt(encrypted)
    
    # Зашифровать JSON
    encrypted_json = encryption.encrypt_json({"key": "value"})
    
    # Расшифровать JSON
    decrypted_json = encryption.decrypt_json(encrypted_json)
```

## 🔧 Настройка Переменных Окружения

Проект требует ровно 5 переменных окружения (см. `.env.example`):

```env
SUPABASE_BASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key_200plus_chars
SUPABASE_ENCRYPTION_KEY=your_fernet_key
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
ADMIN_USER_ID=123456789
```

## 📊 Обновление Базы Данных

### Миграция для Зашифрованных Полей

Выполните SQL-скрипт `supabase_migration_encrypted_fields.sql` в Supabase:

```sql
-- Добавление зашифрованных полей в таблицу strategies
ALTER TABLE strategies 
ADD COLUMN IF NOT EXISTS api_keys_encrypted TEXT,
ADD COLUMN IF NOT EXISTS secret_keys_encrypted TEXT,
ADD COLUMN IF NOT EXISTS private_params_encrypted TEXT,
ADD COLUMN IF NOT EXISTS credentials_encrypted TEXT;
```

## 🚀 Интеграция в bot.py

### Обновленные Функции

**При запуске (`on_startup`):**
- ✅ Валидация конфигурации
- ✅ Проверка подключения к Supabase
- ✅ Отправка уведомления о запуске через `NotificationService`
- ✅ Уведомление всех администраторов

**При остановке (`on_shutdown`):**
- ✅ Уведомление о остановке через `NotificationService`
- ✅ Уведомление всех администраторов

**При ошибках (`main`):**
- ✅ Автоматическое уведомление о критических ошибках
- ✅ Логирование всех ошибок

## 🔐 Архитектура Безопасности

### 1. Использование Service Role Key

Admin Core использует `SUPABASE_SERVICE_KEY` (или `SUPABASE_KEY`) для:
- ✅ Полного доступа к базе данных (обход RLS)
- ✅ Создания и управления стратегиями
- ✅ Управления пользователями
- ✅ Чтения/записи всех данных

**ВАЖНО:** Service Role Key должен быть только в Admin Core. UI/Trading Bot (Repo 01) использует `NEXT_PUBLIC_SUPABASE_KEY` с ограниченным доступом.

### 2. Шифрование Конфиденциальных Данных

Все конфиденциальные данные стратегий шифруются с использованием:
- **Алгоритм:** Fernet (symmetric encryption)
- **Ключ:** `SUPABASE_ENCRYPTION_KEY` из переменных окружения
- **Поля:** API keys, secret keys, private parameters, credentials

### 3. Независимость Компонентов

Admin Core полностью автономен:
- ✅ Не зависит от UI бота
- ✅ Использует собственный токен Telegram
- ✅ Имеет собственную конфигурацию
- ✅ Работает независимо от других компонентов

## 📈 Итоговый Результат

### Выполнено:

1. ✅ **Сервис уведомлений** (`notification_service.py`)
   - Отправка уведомлений администратору
   - Категоризация по уровням важности
   - Интеграция с Telegram Bot API

2. ✅ **Сервис управления стратегиями** (`strategy_manager_service.py`)
   - Создание стратегий с шифрованием
   - Активация/деактивация стратегий
   - Чтение с расшифровкой
   - Интеграция с уведомлениями

3. ✅ **Сервис шифрования** (`EncryptionService`)
   - Шифрование/расшифровка данных
   - Поддержка JSON
   - Обработка ошибок

4. ✅ **Интеграция в bot.py**
   - Уведомления при старте/остановке
   - Уведомления о критических ошибках
   - Валидация конфигурации

5. ✅ **Обновление базы данных**
   - Добавлены поля для зашифрованных данных
   - Создан миграционный скрипт
   - Обновлена основная схема

6. ✅ **Конфигурация**
   - Зафиксированы обязательные переменные окружения (5 шт.)
   - Обновлена валидация настроек
   - Добавлена библиотека `cryptography`

## 🎉 Статус

**Admin Core готов к использованию!**

Система полностью функциональна и готова к:
- 📊 Управлению торговыми стратегиями
- 🔐 Безопасному хранению конфиденциальных данных
- 📢 Отправке системных уведомлений
- 🚀 Интеграции с Trading Core (Bot-1)

## 📝 Следующие Шаги

1. **Настройка окружения:**
   - Установить `ADMIN_USER_ID` в `.env`
   - Сгенерировать и установить `SUPABASE_ENCRYPTION_KEY`
   - Проверить `SUPABASE_SERVICE_KEY` (или `SUPABASE_KEY`)

2. **Обновление базы данных:**
   - Выполнить миграционный скрипт `supabase_migration_encrypted_fields.sql`

3. **Тестирование:**
   - Запустить бота и проверить уведомления
   - Создать тестовую стратегию
   - Проверить шифрование/расшифровку

4. **Интеграция:**
   - Добавить UI для управления стратегиями
   - Интегрировать с Trading Bot для получения стратегий
   - Настроить автоматическое применение активных стратегий

---

**Дата:** 11 декабря 2025  
**Версия:** 1.0  
**Статус:** ✅ Завершено
