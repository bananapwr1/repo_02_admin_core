# 🚀 Быстрая инструкция по деплою Admin Core Bot

## Шаг 1: Подготовка переменных окружения

Вам понадобятся 3 значения:

1. **TELEGRAM_BOT_TOKEN_ADMIN** - Токен бота от @BotFather
2. **SUPABASE_URL** - URL вашего проекта Supabase
3. **SUPABASE_KEY_FOR_ADMIN** - Service role key из Supabase

## Шаг 2: Загрузка в GitHub

```bash
# Если репозиторий еще не создан:
git init
git add .
git commit -m "Initial commit: Admin Core Bot"
git branch -M main
git remote add origin https://github.com/ваш_username/repo_02_admin_core.git
git push -u origin main
```

## Шаг 3: Деплой в Bothost

1. Зайдите на https://bothost.ru
2. Нажмите "Создать бота"
3. Выберите:
   - Тип: **Telegram**
   - Язык: **Python**
4. Укажите URL репозитория: `https://github.com/ваш_username/repo_02_admin_core.git`
5. В разделе "Переменные окружения" добавьте:
   ```
   TELEGRAM_BOT_TOKEN_ADMIN=ваш_токен
   SUPABASE_URL=ваш_url
   SUPABASE_KEY_FOR_ADMIN=ваш_ключ
   ```
6. Нажмите "Создать"

## Шаг 4: Проверка работы

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Отправьте команду `/status` - вы должны увидеть:
   ```
   ✅ Telegram Bot: Подключен
   ✅ Supabase: Подключен
   ```

## Шаг 5: Автообновление (опционально)

1. В GitHub: Settings → Webhooks → Add webhook
2. URL: скопируйте из интерфейса Bothost (раздел "Автообновление")
3. Content type: `application/json`
4. Events: `Just the push event`
5. Сохраните

Теперь при каждом push в GitHub бот будет автоматически обновляться!

## 🐛 Проблемы?

### Ошибка: "supabase_url is required"
✅ **Исправлено!** Теперь бот правильно читает переменные.

### Бот не запускается
1. Проверьте логи в Bothost (вкладка "Логи работы")
2. Убедитесь, что все 3 переменные окружения установлены
3. Проверьте, что токен Telegram правильный

### Бот запускается, но не отвечает
1. Найдите вашего бота в Telegram по имени
2. Отправьте `/start`
3. Если не отвечает - проверьте логи

## 📊 Мониторинг

- **CPU**: Должен быть < 5% в режиме ожидания
- **Память**: Обычно 30-50 MB
- **Логи**: Должны показывать регулярные `HTTP Request: POST ...getUpdates`

## ✅ Готово!

Теперь ваш Admin Core Bot работает и готов к управлению торговыми стратегиями!

Следующий шаг: Деплой UI-бота (`repo_01_ui_bot`)
