# 🎛 Telegram Admin Panel Bot (Bot-2)

Полнофункциональный Telegram-бот для администраторов торговой системы. Служит командным центром для управления Trading Core (Bot-1), пользователями и разработки торговых стратегий.

## 🎯 Основные возможности

### 👥 Управление пользователями
- Просмотр списка всех зарегистрированных пользователей
- Блокировка/разблокировка пользователей
- Управление подписками (VIP, Long/Short, Trial)
- Поиск пользователей по ID
- Просмотр статистики по пользователям

### 🎯 Управление стратегиями
- Просмотр всех торговых стратегий
- Активация/деактивация стратегий
- Создание стратегий через мастер (пошагово) и редактирование JSON индикаторов

### 🎫 Система токенов приглашения
- Генерация одноразовых токенов
- Многоразовые токены (с ограничением использований)
- Неограниченные токены
- Привязка типа подписки к токену
- Просмотр истории использования

### 📊 Мониторинг и логирование
- Системные логи Bot-1
- Логи принятия решений Ядра (reasoning logs)
- Общая статистика системы
- Детальная статистика по пользователям

### ⚙️ Настройки
- Конфигурация названия бота
- Настройка приветственных сообщений
- Системная информация

## 🏗 Архитектура проекта

```
/workspace/
├── bot.py                      # Главный файл запуска бота
├── requirements.txt            # Зависимости проекта
├── .env                        # Конфигурация (не в git!)
├── .env.example               # Пример конфигурации
│
├── config/                    # Конфигурация
│   ├── __init__.py
│   └── settings.py           # Настройки из .env
│
├── database/                  # Работа с Supabase
│   ├── __init__.py
│   └── supabase_connector.py # Класс для всех операций с БД
│
├── handlers/                  # Обработчики команд
│   ├── __init__.py
│   ├── start_handler.py      # /start, /help, /stats
│   ├── users_handler.py      # Управление пользователями
│   ├── strategies_handler.py # Управление стратегиями
│   ├── tokens_handler.py     # Генерация токенов
│   ├── logs_handler.py       # Просмотр логов
│   └── settings_handler.py   # Настройки бота
│
├── keyboards/                 # Клавиатуры
│   ├── __init__.py
│   └── admin_keyboards.py    # Все inline-клавиатуры
│
├── middleware/                # Middleware
│   ├── __init__.py
│   └── admin_middleware.py   # Проверка прав админа
│
├── services/                  # Бизнес-логика
│   ├── __init__.py
│   ├── strategy_manager_service.py # Управление стратегиями + шифрование
│   └── trading_core_service.py     # Автономное ядро генерации сигналов
│
└── utils/                     # Утилиты
    ├── __init__.py
    ├── formatters.py         # Форматирование данных
    └── validators.py         # Валидация входных данных
```

## 🚀 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
nano .env
```

Обязательные переменные:
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `SUPABASE_BASE_URL` - URL вашего Supabase проекта
- `SUPABASE_SERVICE_KEY` (или `SUPABASE_KEY`) - Service Role Key из Supabase (200+ символов)
- `SUPABASE_ENCRYPTION_KEY` - ключ Fernet для шифрования приватных полей
- `ADMIN_USER_ID` - Telegram ID единственного администратора

### 3. Запуск бота

```bash
python3 bot.py
```

## 📊 Структура базы данных Supabase

Бот ожидает следующие таблицы в Supabase:

### `users`
- `telegram_id` (int8, primary key)
- `username` (text)
- `email` (text)
- `is_blocked` (bool)
- `subscription_type` (text)
- `subscription_expires_at` (timestamp)
- `created_at` (timestamp)

### `strategies`
- `id` (int8, primary key)
- `name` (text)
- `description` (text)
- `is_active` (bool)
- `assets_to_monitor` (jsonb)
- `timeframe` (text)
- `indicators` (jsonb)
- `entry_rules` (jsonb)
- `exit_rules` (jsonb)
- `risk_management` (jsonb)
- `created_at` (timestamp)
- `created_by_ai` (bool, legacy; не используется)

### `invite_tokens`
- `token` (text, primary key)
- `max_uses` (int4)
- `current_uses` (int4)
- `subscription_type` (text)
- `is_active` (bool)
- `created_by` (int8)
- `created_at` (timestamp)

### `system_logs`
- `id` (int8, primary key)
- `level` (text)
- `message` (text)
- `details` (jsonb)
- `created_at` (timestamp)

### `decision_logs`
- `id` (int8, primary key)
- `asset` (text)
- `signal_type` (text)
- `reasoning` (text)
- `confidence` (float8)
- `created_at` (timestamp)

### `signals`
- `id` (int8, primary key)
- `asset` (text)
- `signal_type` (text)
- `created_at` (timestamp)

### `trades`
- `id` (int8, primary key)
- `user_id` (int8)
- `asset` (text)
- `created_at` (timestamp)

### `bot_settings`
- `id` (int8, primary key)
- `name` (text)
- `welcome_message` (text)
- `updated_at` (timestamp)

## 🧠 Логика Анализа Ядра (reasoning logs)

- Ядро Admin Core принимает решения **LONG/SHORT/HOLD** строго по шаблонным условиям индикаторов активной стратегии.
- Каждое решение сохраняется в Supabase в таблицу `decision_logs` и доступно в меню **"🧠 Логика Анализа Ядра"**.

## 🔒 Безопасность

- Middleware проверяет ID администратора при каждом запросе
- Все действия админов логируются
- Токены генерируются криптографически безопасным методом
- Чувствительные данные хранятся в .env (не в репозитории)

## 📝 Команды бота

### Основные команды
- `/start` - Главное меню
- `/help` - Справка по командам
- `/stats` - Быстрая статистика

### Кнопки главного меню
- 🎯 **Управление Стратегиями** - создание/редактирование/активация стратегий
- 🧠 **Логика Анализа Ядра** - последние решения/рассуждения (decision logs)
- ⚙️ **Настройки Бота Ядра** - ключи/секреты и системная информация

## 🔧 Разработка

### Добавление новых функций

1. Создайте новый handler в `handlers/`
2. Добавьте клавиатуры в `keyboards/admin_keyboards.py`
3. Зарегистрируйте router в `handlers/__init__.py`

### Добавление новых таблиц Supabase

1. Создайте таблицу в Supabase
2. Добавьте методы в `database/supabase_connector.py`
3. Используйте методы в handlers

## 🐛 Отладка

Логи сохраняются в файл `admin_bot.log` и выводятся в консоль.

Уровни логирования:
- `INFO` - Обычные события
- `WARNING` - Предупреждения
- `ERROR` - Ошибки
- `DEBUG` - Подробная отладочная информация

## 📄 Лицензия

Проект разработан для управления Trading Core Bot.

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Правильность токенов в `.env`
2. Подключение к Supabase
3. Наличие всех необходимых таблиц в БД
4. `ADMIN_USER_ID` в `.env`
5. Логи в файле `admin_bot.log`

---

**Версия:** 1.0.0
**Дата:** 2025-12-11
