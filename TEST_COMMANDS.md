# 🧪 Тестовые команды для проверки бота

После деплоя используйте эти команды для проверки всех функций бота.

## 📋 Порядок тестирования

Выполняйте команды в указанном порядке:

---

## ✅ Тест 1: Базовая проверка

### Команда: `/start`

**Отправьте:**
```
/start
```

**Ожидаемый ответ:**
```
🤖 Admin Bot для управления торговыми стратегиями

Доступные команды:
/start - Показать это сообщение
/ai_run - Запустить генерацию новой стратегии
/status - Проверить статус подключений
/view_strategies - Показать все стратегии
```

**Статус:**
- ✅ Бот отвечает
- ✅ Форматирование правильное
- ✅ Все команды перечислены

**Если не работает:**
- Проверьте, что бот запущен в Bothost
- Проверьте логи работы
- Убедитесь, что токен правильный

---

## ✅ Тест 2: Проверка подключений

### Команда: `/status`

**Отправьте:**
```
/status
```

**Ожидаемый ответ (успешный):**
```
📊 Статус системы:

✅ Telegram Bot: Подключен
✅ Supabase: Подключен (стратегий: 0)

🔐 Переменные окружения:
TELEGRAM_BOT_TOKEN_ADMIN: ✅
SUPABASE_URL: ✅
SUPABASE_KEY: ✅
```

**Возможные варианты:**

#### Вариант А: Всё отлично (все ✅)
```
✅ Telegram Bot: Подключен
✅ Supabase: Подключен (стратегий: 0)
✅✅✅ Все переменные
```
👉 **Отлично! Переходите к тесту 3**

#### Вариант Б: Supabase не подключен
```
✅ Telegram Bot: Подключен
❌ Supabase: Не подключен

🔐 Переменные окружения:
TELEGRAM_BOT_TOKEN_ADMIN: ✅
SUPABASE_URL: ❌
SUPABASE_KEY: ❌
```
👉 **Проблема:** Проверьте переменные в Bothost

#### Вариант В: Ошибка Supabase
```
✅ Telegram Bot: Подключен
⚠️ Supabase: Ошибка - Invalid API key
```
👉 **Проблема:** Неправильный SUPABASE_KEY

**Решение проблем:**

1. Откройте Bothost → Настройки бота
2. Проверьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN_ADMIN` - токен от @BotFather
   - `SUPABASE_URL` - https://your-project.supabase.co
   - `SUPABASE_KEY_FOR_ADMIN` - service_role key (НЕ anon!)
3. Перезапустите бота
4. Попробуйте `/status` снова

---

## ✅ Тест 3: Просмотр стратегий

### Команда: `/view_strategies`

**Отправьте:**
```
/view_strategies
```

**Ожидаемый ответ (если база пустая):**
```
📭 Стратегий пока нет в базе данных.
```

**Ожидаемый ответ (если есть стратегии):**
```
📋 Активные стратегии:

🟢 Strategy_1234567890
  Assets: EURUSD=X, GBPJPY=X
  Amount: $15.0
  Timeframe: 120s
  Rules: RSI < 30 AND MACD Cross UP

⚪ Old_Strategy_123
  Assets: BTCUSD
  Amount: $10.0
  Timeframe: 60s
  Rules: SMA Cross
```

**Легенда:**
- 🟢 = Активная стратегия (is_active: true)
- ⚪ = Неактивная стратегия (is_active: false)

**Статус:**
- ✅ Команда работает
- ✅ Данные из Supabase читаются
- ✅ Форматирование корректное

**Если не работает:**
```
❌ Supabase не подключен.
```
👉 Вернитесь к Тесту 2 и исправьте подключение

---

## ✅ Тест 4: Генерация стратегии (опционально)

### Команда: `/ai_run`

⚠️ **Внимание:** Эта команда записывает данные в Supabase!

**Отправьте:**
```
/ai_run
```

**Ожидаемый ответ (успешный):**
```
⏳ Запускаю тяжелый процесс AI-анализа и генерации новой стратегии. 
   Это может занять несколько минут...

[через несколько секунд]

✅ Генерация стратегии завершена! 
   Ядро Render начнет использовать новый алгоритм в следующем цикле.
```

**После этого проверьте:**

1. Отправьте `/view_strategies`
2. Вы должны увидеть новую стратегию с именем `Strategy_[timestamp]`
3. Она должна быть помечена как активная (🟢)

**Возможные ошибки:**

#### Ошибка А: Нет подключения
```
❌ Supabase не подключен. Проверьте настройки переменных окружения.
```
👉 Исправьте переменные и попробуйте снова

#### Ошибка Б: Ошибка записи
```
⏳ Запускаю тяжелый процесс...
❌ Ошибка при генерации стратегии: [error message]
```
👉 Проверьте логи в Bothost для детальной информации

---

## 🎯 Полная таблица проверки

| # | Команда | Статус | Примечания |
|---|---------|--------|-----------|
| 1 | `/start` | ☐ | Базовая проверка |
| 2 | `/status` | ☐ | Все ✅ ? |
| 3 | `/view_strategies` | ☐ | Читает из БД? |
| 4 | `/ai_run` | ☐ | Опционально |

**Отметьте ✅ каждую проверенную команду**

---

## 🔍 Дополнительные проверки

### Проверка 5: Логи в Bothost

1. Откройте Bothost
2. Найдите `repo_02_admin_core`
3. Откройте "Логи работы"
4. Должны видеть:

```
INFO:__main__:🚀 Starting Admin Bot...
INFO:__main__:✅ Supabase client initialized successfully
INFO:__main__:✅ Admin Bot started successfully!
INFO:httpx:HTTP Request: POST https://api.telegram.org/bot.../getUpdates "HTTP/1.1 200 OK"
```

**Что это значит:**
- `🚀 Starting Admin Bot...` - Бот запускается
- `✅ Supabase client initialized` - Подключение к БД успешно
- `✅ Admin Bot started successfully` - Все обработчики зарегистрированы
- `HTTP Request: ...getUpdates` - Бот опрашивает Telegram API

### Проверка 6: Мониторинг ресурсов

В Bothost проверьте:
- **CPU**: Должен быть < 5% в простое
- **Память**: 30-50 MB обычно
- **Статус**: "Работает" (зелёная иконка)

---

## 🐛 Что делать если всё сломалось

### Сценарий 1: Бот вообще не отвечает

**Проверьте:**
1. Статус бота в Bothost (должен быть "Работает")
2. Логи работы (есть ли ошибки?)
3. Переменные окружения (все 3 заданы?)
4. Правильный ли бот в Telegram? (проверьте @username)

**Решение:**
```bash
# В Bothost нажмите "Перезапустить"
# Подождите 30 секунд
# Попробуйте /start снова
```

### Сценарий 2: Только одна команда не работает

**Проверьте логи:** Должна быть ошибка с конкретной командой

**Решение:**
```bash
# Проверьте CHANGELOG.md
# Убедитесь, что pull последний код
# Перезапустите бота
```

### Сценарий 3: Ошибки Supabase

**Типичные ошибки:**

```
Invalid API key
```
👉 Используйте service_role key, НЕ anon key

```
Table 'strategy_settings' does not exist
```
👉 Создайте таблицу в Supabase

```
Insufficient privileges
```
👉 Используйте service_role key с полными правами

---

## ✅ Критерии успеха

Бот работает правильно, если:

- ✅ Отвечает на все 4 команды
- ✅ `/status` показывает все ✅
- ✅ `/view_strategies` читает из БД
- ✅ `/ai_run` создаёт новую стратегию
- ✅ Логи чистые (нет ERROR)
- ✅ CPU < 5%, Memory < 100MB
- ✅ Статус "Работает" в Bothost

---

## 🎉 Всё работает?

**Поздравляем! Ваш Admin Core Bot полностью функционален!**

### Следующие шаги:

1. ✅ Настройте webhook для автообновлений (DEPLOY.md)
2. ✅ Деплойте UI-бота (`repo_01_ui_bot`)
3. ✅ Интегрируйте с Core Engine на Render
4. ✅ Начинайте использовать!

---

## 📊 Быстрая диагностика

Используйте эту таблицу для быстрой проверки:

| Проблема | Команда для диагностики | Решение |
|----------|------------------------|---------|
| Бот не отвечает | Проверить статус в Bothost | Перезапустить |
| Supabase не подключен | `/status` | Проверить переменные |
| Стратегии не видны | `/view_strategies` | Проверить таблицу в Supabase |
| Ошибка при /ai_run | Логи в Bothost | Проверить CHANGELOG.md |

---

**Удачного тестирования! 🚀**
