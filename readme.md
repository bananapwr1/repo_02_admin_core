# 🤖 Admin Core Bot

Telegram-бот для управления торговыми стратегиями и AI-анализом.

## 📋 Описание

Admin Core Bot отвечает за:
- Генерацию новых торговых стратегий на основе AI-анализа
- Управление стратегиями в базе данных Supabase
- Мониторинг статуса системы
- Просмотр активных стратегий

## 🚀 Деплой на Bothost

### 1. Настройка переменных окружения в Bothost:

В интерфейсе Bothost добавьте следующие переменные:

```
TELEGRAM_BOT_TOKEN_ADMIN=your_admin_bot_token
SUPABASE_URL=your_supabase_url
SUPABASE_KEY_FOR_ADMIN=your_supabase_service_key
```

### 2. Настройка GitHub репозитория:

1. Создайте репозиторий `repo_02_admin_core` на GitHub
2. Загрузите все файлы из этой папки
3. В Bothost подключите репозиторий через URL

### 3. Автообновление:

Настройте webhook в GitHub для автоматического обновления при push:
- Settings → Webhooks → Add webhook
- Вставьте URL из Bothost
- Content type: `application/json`

## 📦 Зависимости

```
python-telegram-bot
python-dotenv
supabase
pandas
```

## 💻 Команды бота

- `/start` - Показать приветственное сообщение
- `/status` - Проверить статус всех подключений
- `/ai_run` - Запустить генерацию новой стратегии
- `/view_strategies` - Показать все активные стратегии

## 🛠️ Устранение неполадок

### Ошибка: "supabase_url is required"
- Убедитесь, что переменные окружения указаны **точно** как в примере выше
- Перезапустите бота в Bothost после изменения переменных

### Бот не отвечает на команды
- Проверьте логи работы в Bothost
- Убедитесь, что токен Telegram бота корректный
- Выполните команду `/status` для диагностики

## 📊 Архитектура

```
Admin Core Bot (Bothost)
    ↓
Supabase Database
    ↓
Core Trading Engine (Render)
    ↓
Trading Platforms
```

## 🔒 Безопасность

- Никогда не коммитьте файл `.env` в публичный репозиторий
- Используйте переменные окружения Bothost для хранения секретов
- SUPABASE_KEY_FOR_ADMIN должен быть service_role key с полными правами

## 📝 Примечания

- Бот работает в режиме polling (постоянный опрос Telegram API)
- Генерация стратегий может занимать несколько минут
- Для production рекомендуется использовать webhook вместо polling

## 🔍 Проверка работоспособности

После деплоя:
1. Откройте бота в Telegram
2. Отправьте команду `/start`
3. Отправьте команду `/status` для проверки подключений
4. Если всё работает, вы увидите зелёные галочки ✅

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Bothost (вкладка "Логи работы")
2. Убедитесь, что все переменные окружения установлены
3. Обратитесь в Telegram-группу поддержки Bothost
